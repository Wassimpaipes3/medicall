import 'package:flutter/material.dart';
import '../../screens/provider/provider_navigation_screen.dart';

/// Main provider navigation entry point that uses the provider navigation screen
class ProviderMainNavigation extends StatelessWidget {
  const ProviderMainNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const ProviderNavigationScreen();
  }
}
